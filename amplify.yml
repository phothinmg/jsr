version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands: [vite build]
  artifacts:
    baseDirectory: '.'
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
